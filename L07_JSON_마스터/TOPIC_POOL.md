# 🎯 화두 풀 — JSON 마스터

---

## 🟢 입문

### 1. 레시피 데이터베이스 ⭐ 추천
> "좋아하는 요리 5개의 레시피를 JSON으로: 요리명, 재료(배열), 난이도, 조리시간, 단계(배열)"

### 2. 플레이리스트 관리
> "음악 플레이리스트를 JSON으로: 곡명, 가수, 장르, 재생시간. 장르별 필터링"

### 3. 게임 세이브 데이터
> "RPG 캐릭터 세이브를 JSON으로: 이름, 레벨, HP/MP, 인벤토리(배열), 장비(객체)"

### 4. 환경설정 관리자
> "앱 설정을 JSON으로 관리: 테마, 언어, 알림 설정, 글꼴 크기. 설정 변경+저장"

### 5. 연락처 관리 (JSON 버전)
> "6강 CSV 연락처를 JSON으로 변환하고, 검색/추가/삭제 기능"

---

## 🟡 도전

### 6. 퀴즈 데이터 설계 ⭐ 추천
> "4지선다 퀴즈 10문제를 JSON으로 설계: 문제, 보기(배열), 정답, 난이도, 카테고리"

### 7. 가계도 트리
> "가족 관계를 JSON 트리 구조로 표현 (부모-자식 중첩)"

### 8. 다국어 번역 파일
> "앱 UI 문구를 한국어/영어/일본어로 관리하는 다국어 JSON 파일"

### 9. 일정표 데이터
> "주간 일정을 JSON으로 관리: 요일별 시간대, 제목, 장소, 메모"

### 10. API 응답 파싱
> "날씨 API 응답 예시 JSON을 파싱하여 필요한 정보만 추출하는 프로그램"

---

## 🔴 심화

### 11. JSON 스키마 검증
> "JSON 데이터가 정해진 형식에 맞는지 검증하는 프로그램"

### 12. JSON 병합 도구
> "여러 JSON 파일을 하나로 합치되, 중복 키는 최신 값 유지"

### 13. JSON → 마크다운 변환기
> "JSON 데이터를 보기 좋은 마크다운 문서로 자동 변환"

---

## 💡 실생활 응용

### 14. 독서 기록
> "읽은 책, 읽는 중인 책, 읽고 싶은 책을 상태별로 관리하는 JSON 시스템"

### 15. 여행 계획
> "여행지별 볼거리, 맛집, 숙소를 JSON으로 체계적 정리"

---

## 🤔 생각해볼 점

- JSON 파일이 너무 커지면 어떻게 관리할까? → 데이터베이스의 필요성 (23강 예고)
- JSON에서 주석을 쓸 수 없는 이유와 대안은?
- 같은 데이터를 CSV와 JSON으로 저장할 때 파일 크기 차이는?
