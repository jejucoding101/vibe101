# 🎯 화두 풀 — 데이터베이스

---

## 🟢 입문
1. **회원 관리 DB** ⭐ — "이름, 이메일, 가입일, 레벨 테이블 CRUD"
2. **도서관 시스템** — "도서(제목, 저자, ISBN, 대출상태) 관리"
3. **가계부 DB** — "수입/지출 내역 저장 + 월별 합계 쿼리"
4. **연락처 DB** — "전화번호부 CRUD + 이름으로 검색"
5. **할일 DB** — "할일 목록 + 완료 여부 + 마감일"

## 🟡 도전
6. **게시물+댓글 관계** ⭐ — "게시물 테이블 + 댓글 테이블 (외래키 관계)" 
7. **태그 시스템** — "글에 여러 태그를 붙이는 다대다(N:M) 관계"
8. **주문 관리** — "고객, 상품, 주문 3개 테이블 관계"
9. **채팅 로그** — "채팅 메시지 저장 + 날짜/키워드 검색"
10. **검색 기능 강화** — "LIKE, 와일드카드를 활용한 전문 검색"

## 🔴 심화
11. **데이터 마이그레이션** — "기존 JSON 데이터를 DB로 일괄 이전하는 스크립트"
12. **인덱스와 성능** — "데이터 10만 건에서 인덱스 유무에 따른 검색 속도 비교"
13. **트랜잭션** — "여러 쿼리를 하나의 단위로 묶어 실행 (성공/실패 일괄 처리)"

## 💡 실생활
14. **센서 데이터 로깅** — "온도/습도 등 센서 데이터를 주기적으로 DB에 기록" (Python 추천)
15. **설문 결과 DB** — "설문 응답을 DB에 저장하고 통계 쿼리로 분석"

---

## 🤔 생각해볼 점
- JSON 파일과 SQLite의 장단점을 비교해보세요
- 테이블을 어떻게 나눠야 할까? (정규화의 개념)
- "WHERE id = 1" vs "WHERE name = '홍길동'" → 인덱스의 중요성
